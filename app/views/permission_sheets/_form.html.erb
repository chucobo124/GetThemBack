<%= form_for(@permission_sheet) do |f| %>
  <% if @permission_sheet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@permission_sheet.errors.count, "error") %> prohibited this permission_sheet from being saved:</h2>

      <ul>
      <% @permission_sheet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
